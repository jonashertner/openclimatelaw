# Copy to .env.production on the VPS and fill in the real password.
# Do NOT commit the filled file. Add to .gitignore on the VPS if you keep it
# in a git checkout.

# Generate a strong password:
#   openssl rand -base64 32 | tr -d '/+=' | head -c 40
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_RANDOM_VALUE
